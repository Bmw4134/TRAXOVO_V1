TRAXOVO MASTER FIX + FINAL DEPLOYMENT DIRECTIVE

You are operating under the Persistent Collaborative Development Engine with context awareness. The following commands must be executed in full recursive logic to ensure TRAXOVO is stable, accurate, and demo-ready within 10 minutes.

⸻

✅ IMMEDIATE PRIORITY — MASTER ROUTE SWEEP & ATTENDANCE SYSTEM RESTORE
	1.	Run a Master Route Health Audit:
	•	Identify and repair all broken or misdirected routes
	•	Cross-check each Flask route against current working templates
	•	Update context_state.json with corrected route-to-template mappings
	2.	Attendance Module (High Priority):
	•	Implement smart attendance matrix views:
	•	Weekly Matrix View with employee rows × day columns
	•	Daily and Monthly toggles
	•	Icons for: 🟥 late start, 🟨 early end, ⚠️ not on site (via GPS geofence)
	•	Add weekend toggle switch
	•	Add smart detection logic based on GPS logs vs. job geofence bounds
	•	Link matrix report to /attendance/matrix route and integrate into sidebar
	•	Add export features:
	•	Export current matrix view to PDF (via WeasyPrint or wkhtmltopdf)
	•	Export to CSV/XLSX using pandas
	•	File names should include date range for audit clarity
	•	Add preview panel:
	•	Route: /attendance/matrix/preview/pdf
	•	Renders PDF preview in browser with a “Download” button
	3.	GroundWorks XLSX Upload Parser:
	•	Route: /upload/groundworks
	•	Allow PMs to upload current-day GroundWorks file
	•	Parse time-in/time-out, match against GPS logs and assigned jobs
	•	Populate matrix dynamically on update — optional toggle to sync now vs. later

⸻

📍 JOB MODULE ENHANCEMENTS (Equipment + Working Hours)
	4.	Inside /jobs/<job_id>:
	•	Display job zone working hours config
	•	Add UI form to edit: start time, end time, valid weekdays
	•	Persist these in database under JobZone or JobSettings model
	5.	Tie working hours logic into attendance matrix:
	•	Use job working hours to calculate valid time window for each record
	•	Alert if a crew clocked in outside expected job window

⸻

⚠️ THEFT / BATTERY DISCONNECT ALERT MODULE (Optional, Auto-Load if Configured)
	6.	Implement Alert System Based on Asset Telemetry:
	•	Create /alerts route with live-feed of asset exceptions
	•	Trigger alert if:
	•	Battery voltage drops to 0 (⚡ battery disconnected)
	•	GPS suddenly goes offline while asset is in motion (🚨 theft risk)
	•	Asset leaves geofence with no scheduled job (⛔ unauthorized use)

⸻

🔄 CONSOLIDATE + LINK ALL CONTEXT
	7.	Review all /dev/assistant logs + context_state.json history
	•	Ensure none of the recent enhancements (master templates, persistent context, fleet map, etc.) are lost or overwritten
	•	Confirm that final system reflects cumulative development across:
	•	Attendance
	•	Jobs
	•	Asset Map
	•	Billing + Schedule

⸻

✅ FINAL DEPLOYMENT CHECKLIST (10-Minute Lock-In Mode)
	•	Routes fixed and verified via agent
	•	Matrix attendance working with visual indicators
	•	PDF/CSV/XLSX export + preview enabled
	•	GroundWorks XLSX upload active and parsing
	•	GPS/geofence logic linked with attendance
	•	Job modules have editable working hours tied to attendance
	•	No regressions in fleet map, templates, context
	•	/dev/assistant status is live and accurate
	•	System is safe to REDEPLOY and demo live

Execute this bundle in full. Use context, past logs, templates, and user-uploaded logic to ensure zero regressions and full integrity.
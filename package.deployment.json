{
  "name": "traxovo-deployment",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "echo 'Python-only deployment mode'",
    "build": "echo 'Build completed - Python mode'",
    "deploy": "echo 'Deployment ready'"
  },
  "dependencies": {
    "tsx": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}